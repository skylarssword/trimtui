# trimtui default settings
# These values pre-fill the menus when using Custom mode.
# Edit freely — trimtui reads this on every launch.

resolution=original
fps=original
codec=libx264
crf=22
enc_preset=medium
audio=copy
